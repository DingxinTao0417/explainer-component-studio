# 组件与动画库 V3

当前实现包含 **78 个可编辑组件、100 种动画、13 个音效预设**。原创讲解图形共 30 个；新增 24 个 Apple 界面。保留白底、蓝色强调、薄荷绿，以及原有 Codex、Windows 和开发工具组件。

[打开画廊](http://localhost:3031/catalog.html) · [直接播放参考舞台](http://localhost:3031/catalog.html?scene=reference-stage) · [完整目录](CATALOG.md) · [参考片拆解](../analysis/reference-03/视频拆解与升级规格.md)

## 直接复用

1. 在画廊选择组件，修改右侧 JSON，点击“应用内容”。
2. 选择动画；课件舞台还可以叠加“常驻背景”。背景默认无声，动作音可开关和调节音量。
3. 下载“场景配置”。该文件包含内容、动画、背景和音量；画廊试改不会自动写入磁盘。
4. 用下面命令生成可接入 HyperFrames 的 HTML，不编码视频：

```powershell
node scripts/export-scene.mjs examples/reference-stage-scene.json --out examples/reference-stage.html
```

输出已存在时改用新文件名，或显式加 `--overwrite`。参考场景配置已配好滚动透视网格、分层弧带、圆形装饰、虚线边框和 A/B 两段内容。

## 10 类动画，每类 10 种

| 分类 | 用法举例 |
|---|---|
| 入场 | 淡入、窗口滑入、由虚到实、发牌、从中央展开 |
| 操作 | 逐字输入、点击、滚动、开关、拖放、滑块调节 |
| 标注 | 聚光、虚线框、四角定位、引线、编号、关键词标记 |
| 讲解 | 连线、条件分支、循环路径、公式组合、层级构建 |
| 镜头 | 推近、拉远、重点切换、框内移动、前后层视差 |
| 数据 | 计数、柱形、环形占比、散点、热力、雷达 |
| 退场 | 收起、淡出、滑出、圆形收幕、分离退场 |
| 反馈 | 完成勾、通知、已保存、已复制、等待完成、轻粒子 |
| 转场 | 弧带、斜带、同场横移、柔焦交叠、穿越、换页、分栏 |
| 背景 | 网格、透视网格、点阵、边角圆形、曲线、圆环、波带 |

每种动画都有独立机制和演示对象。可用效果依据组件的实际目标筛选，并非任意动画都适用于所有组件。

## 真实 PPT / 录屏放哪里

选 `lecture-stage`，将文件放到工程的 `assets` 目录，修改：

```json
{"media":{"kind":"video","src":"assets/my-screen-recording.mp4","fit":"contain"}}
```

静态课件将 `kind` 改成 `image`。空 `src` 显示可编辑的原生三栏课件。默认保留媒体比例；`cover` 会裁边。HyperFrames 接入后由框架控制媒体时间，浏览器组件画廊主要用于查看造型和动作。

背景在课件外层运动，圆形被裁在两个角落；课件、录屏和字幕承担信息。不要把背景运动同时套到正文上。真实配音和 BGM 应在父时间线保留各自音轨，AI 视频素材默认静音。

## 转场的两幅内容

`props.transitionNext` 指定下一幅画面的组件和内容。示例：

```json
{
  "transitionNext":{
    "component":"lecture-stage",
    "props":{"title":"下一节：开始实践","chapter":"02 / 操作演示"}
  }
}
```

两个画面独立渲染，遮幅类在完全覆盖时交接；不是遮住同一个画面后再显示它。同场横移建议前后都使用 `lecture-stage`，背景保持连续。`effectOptions.background` 可以同时给前后两块舞台添加背景。

默认转场从 2.1 秒开始，基本时长 0.66 秒；色带到位后额外停留 0.025 秒，再依次离场；实际全屏覆盖还包含色带进出时的一小段时间。其他动作大多从 0.6 秒开始，退场在阅读停留后开始。所有演示的时间窗口为 8 秒。配套声音固定在这些时点；改变内部时间需重新配轨，或关闭配套声音。

## Apple 范围与精度

16 个 macOS：访达、Safari、终端、系统设置、聚焦、控制中心、通知、备忘录、日历、邮件、预览、活动监视器、保存对话框、权限提示、右键菜单、程序坞。

6 个 iPhone：设置、信息、Safari、备忘录、控制中心、分享面板。2 个 iPad：分屏工作台、文件 App。

设计版本为 macOS Sequoia 15 / iOS 18 / iPadOS 18。使用官方版本化手册作结构参照；本机字体回退和手绘图标仍与真机有差异，**未声称每个 Apple 组件都逐像素 1:1**。原型依据、实际截图和可编辑实现分别留痕，见 [质量说明](QUALITY.md)。

## 文件与检查

- `content/*.json`：长期保存的组件内容；改完运行 `npm run build`。
- `families/*.mjs`：78 个组件实现。
- `animations.mjs`、`motion-expanded.mjs`：动画、作用目标及声音落点。
- `reports/dynamic-v3/`：每种动画的实时播放帧、12 个关键时点、逆向 seek 结果和目视复核记录。
- `reports/verification.json`：内容替换、HTML 转义、动画状态和画廊功能。
- `reports/audio-verification.json`：13 个声音资产、100 条效果轨加静音轨、播放暂停、拖动及导出接入。

动态检查包含实际浏览器播放和逐项连续画面复核；仍不等于最终视频编码、真机截图差分或与配音混合后的试听。保留原生小字号时，应使用推近和标注改善成片阅读。

本地画廊监听 `127.0.0.1:3031`，使用 `scripts/start-preview.ps1` 启动或复用，`scripts/stop-preview.ps1` 停止。没有提交、推送或公开发布。

要复跑真实视频接入检查，可设置 `HF_TEST_VIDEO` 为任一本地可解码 MP4 的绝对路径，再运行 `node scripts/verify-hyperframes-integration.mjs`。未设置时使用原工程的既有样片；压缩包不附带用户原始参考视频。默认组件原生尺寸为 1280×800，放入 16:9 父画布时请等比缩放并留白，不拉伸软件 UI。
