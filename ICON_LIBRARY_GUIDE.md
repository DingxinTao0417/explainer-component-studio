# 透明图标库

[打开图标库](http://127.0.0.1:3031/catalog.html?tab=icons) · [AI 品牌](http://127.0.0.1:3031/catalog.html?tab=icons&category=AI%20品牌) · [来源与许可](assets/icons/CREDITS.md)

现有 253 个通用图标、170 个 AI 公司／模型／产品标记，共 423 项，含彩色、单色和字标共 711 份 SVG。作为素材独立计数，不计入 138 个组件或 113 种动画。

## 使用

1. 在“组件、动画效果、音效试听、背景样式”旁切换到“图标库”。
2. 选择通用、AI 品牌或更具体的分类；可按中英文名称搜索，例如“豆包”“DeepSeek”“Codex”“文件”“arrow”。切换分类会清除旧搜索。
3. 点击卡片，选择已有的彩色／单色／字标版本。没有的版本不会伪造，也不会将彩色标记滤色冒充官方版本。
4. 可以选择 256、512、1024、2048 像素导出尺寸，以及四周留白。通用图标支持颜色与线宽；品牌单色标记支持颜色。
5. 下载透明 SVG 或透明 PNG；也可复制 SVG 或库内相对素材路径。页面底色只用于查看透明区域，不写入文件。

示例路径：`assets/icons/brand/doubao-color.svg`、`assets/icons/brand/codex.svg`（具体版本以预览框中的“组件素材路径”为准）。复制的路径指向磁盘原始版本；改过颜色、尺寸或线宽时，使用“下载透明 SVG”或“复制 SVG”保留修改。Codex 默认使用透明单色标记；上游的彩色应用图标自带白色底板，已在版本名称中注明。

放入视频工程时，将下载文件复制到该工程的素材目录，再按该组件支持的图片字段引用。图标库不会在用户现有场景中自动插入或替换品牌。

## 维护

常规构建 `node scripts/build.mjs` 不联网，也不会重新下载图标。修改图标选单后运行 `python scripts/import-icons.py` 导入；升级图标包须显式更新版本与 SHA-512 校验值，再做透明度和视觉检查。

页面读取 `icon-data.mjs`，离线 SVG 位于 `assets/icons/`。包名、版本、许可、精确来源、文件校验值均保存在 `assets/icons/catalog.json`；完整许可同时保留在 SVG metadata 和独立许可文件中。

本轮本地完成，不包含提交、推送或部署。图标导出不渲染视频，也不启动模型服务。
