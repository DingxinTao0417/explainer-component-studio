# B-roll 动画插镜

动画插镜分类现有 10 个独立组件，另外保留 4 个真实素材框架。混剪框架的 6 种呈现配方不计入组件数量。

| 组件 ID | 用途 | 主要可编辑内容 |
| --- | --- | --- |
| broll-brief-desk | 文档与便签落位 | 正文、批注、便签、检查项 |
| broll-message-pile | 消息整理为结果 | 消息、来源标签、结果字段 |
| broll-revision-stack | 版本迭代 | 版本名、正文、差异、核对项 |
| broll-document-scan | 扫描与提取 | lines 最多 4 行；fields 最多 3 项 |
| broll-search-focus | 检索与定位 | query；results 最多 3 条；selected 从 0 开始；摘录与来源说明 |
| broll-calendar-pin | 排期落位 | selectedDay 1–28；星期标签；任务与日期说明 |
| broll-folder-sort | 文件归档 | groups 最多 3 组，每组一份文件和一个目录 |
| broll-edit-timeline | 多轨剪辑 | tracks 最多 3 轨、每轨 5 片段；start/end 为 0–1 的相对位置 |
| broll-voice-transcript | 语音转文字 | 音频标题、segments 最多 3 段、时码与文字 |
| broll-focus-timer | 专注与完成 | 开始/结束时间标签、tasks 最多 3 项、结束状态 |

这 10 个组件均接入默认专属动作，画廊预览、效果预览、编导索引和 v5 导出共用实现。所有动作可正反向定位，不在渲染时随机。默认 8 秒，可在编导导出时通过 timing.duration 调整；新增插镜默认静音，音效与音乐由本期编导按实际动作选择。

新增组件是抽象工作场景，不是软件截图。日历是固定四周示意格，不自动计算真实月份；波形是原创示意，不从音频计算，也不生成配音；计时是压缩时间示意；剪辑轨道不播放媒体。不要把这些图形当作真实操作或识别结果的证据。需要展示真实结果时使用用户录屏、实拍、可核实来源与可换素材框架。

标题建议一行，正文以短句为主。新组件保留完整文本，剪辑轨道的短片段名称空间不足时会省略显示；完整名称仍在 props。超过上表容量的数组仅显示前 N 项，更多内容应拆镜头。

在画廊点击组件即可播放或改 JSON，专属效果只提供给对应组件；也可改选兼容的通用动效。正文通过 content/<组件 ID>.json 或项目 props 替换；不要修改 family 源码来填某一期文案。新项目继续先展示组件与实际素材候选，确认后才制作。

真实图片与视频请使用 [可换素材说明](MEDIA_SLOTS_GUIDE.md)，每期重新选取 Pexels、可核实网络来源或用户提供的本地素材。

验证入口：`node scripts/verify-broll-workflows.mjs`。专属动作静音；验证合集也不额外配乐。
