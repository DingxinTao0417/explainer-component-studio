# B-roll 模板是框架，素材由本期内容决定

画廊里的办公图片/视频只是用于看布局和动效的演示填充物，不是模板限定素材，也不是每期默认要采用的素材。同一个框架可以装风景、人物、实验、产品图片、Pexels 实拍视频、自己的录屏或用户回传的视频。

编导先按讲稿寻找适合本期的素材，再选择全屏、并排、画中画、拼贴等表达方式。主画面和每个辅助窗口都能独立替换；局部放大窗跟随主素材，不需要第二份不同素材。换素材保持模板结构，重新核对裁切、焦点、标注和视频源区间。

## 画廊怎么换

打开 B-roll 组件，在“素材可替换”中选素材位置，填写本地相对路径与类型，点“读取素材并替换”。画廊自动读取图片尺寸或视频尺寸/总长，不要求手填像素。默认清除混剪素材上旧的特定焦点和标注；如果同一画面的更新版本要保留这些设置，可勾选保留后自行核对。

“应用框架，保留素材”将当前混剪素材组合为一个镜头，保留总时长和实际来源，不回填演示图片。素材数量与目标框架不符或视频长度不足会提示，不悄悄丢弃或补样图。“查看演示素材示例”里的“载入通用示例”才会加载图库演示素材。

画廊不是全盘文件浏览器：这里的路径必须是当前预览服务可访问的文件。Pexels 或其他网站的素材先下载到本期工程；助手负责选材、落盘、填槽和本期预览，用户不必自己改 JSON。生产使用本期配置及导出包，不能把项目图片写回共享组件 defaults。下载配置会保留已替换的路径。

## 编导填槽

| 组件 | 可替换字段 |
| --- | --- |
| broll-cutaway / broll-detail | mediaSrc、mediaType、mediaAlt、mediaStart |
| broll-sequence | media[0..2].src/type/alt/mediaStart |
| mixed-media-sequence | media[].src/type/width/height/sourceStart/sourceDuration；各 mediaPanels[] 同样可换 |

`director-index.json` 的 mediaSlots 给出接口和“演示素材不是采用素材”的说明。`media-slots.mjs` 的 mediaSlots/replaceMediaSlot 可用于逐槽替换，applyMediaFramework 只重组已有素材；不包含在线搜索或下载服务。原生 v5 JSON 仍可直接编辑，最终尺寸和时长由导出校验。

新项目按主题检索 Pexels；需要其他网站素材时核对实际来源与许可。下载为本地文件并保留具体素材页、作者、许可、用途；网页地址与素材文件地址分开，导出使用本地素材路径而不是搜索结果页或易失效的热链。不能用当前办公样图代替与本期主题无关的待找素材，也不能将示意图片当真实事件/操作证据。

开场方案分别展示“准备使用哪个框架”和“准备填入哪几份实际素材”。素材还没找到就标待补，不能把演示画面登记成已选定。已确认本期素材后，沿用现有完整方案确认与制作流程；换新素材重新检查受影响镜头。
