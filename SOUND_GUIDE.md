# 动画与音效使用说明

这套库提供 **78 个组件、100 种动画和 13 个音效预设**。每种动画都有一条 8 秒时间轨，其中 90 种动作有音效、10 种常驻背景使用真静音轨，点击、输入、提示、完成和转场声按对应动作安排，空隙保留安静。

## 在画廊试听

1. 打开 [本地画廊](http://localhost:3031/catalog.html)，切换到“音效试听”，点击卡片试听单个预设；再次点击停止。切换标签会停止当前试听。
2. 切换到“动画效果”，打开一个效果，勾选右侧“同步播放音效”，用音量滑杆调整配套轨总音量。
3. 首次点击“播放”会从 0 秒开始；“重播”也从头开始。拖动时间轴会暂停声音，适合安静检查画面；再次播放从所选位置继续。
4. 关闭详情窗口会停止声音。取消勾选或把音量调到 0，可单独检查动画。

右侧列出的秒数是声音落点。单个音效试听采用该预设的建议音量，动画播放还经过场景音量控制，两处听到的大小不必相同。

## 保存成下次能直接改的场景

在详情窗口修改 JSON，点击“应用内容”，选好动画、声音开关和音量，再点击“下载场景配置”。这个文件同时保存组件与声音设置；“下载内容”只保存组件文案和数据。

把下载的场景配置放进本工程，例如 `examples/with-sound.json`，然后在组件库目录执行：

```powershell
node scripts/export-scene.mjs examples/with-sound.json --out examples/scene.html
```

输出是可复用的 HyperFrames HTML 场景，不会渲染视频。输出文件若已存在，脚本会停止，避免直接覆盖；换一个输出名称，或在明确需要替换时加 `--overwrite`。

场景配置的主要字段：

| 字段 | 含义 | 示例 |
|---|---|---|
| component | 组件 ID | `terminal-session` |
| props | 本次文案、命令、数据或素材字段 | 从画廊保存的内容对象 |
| effect | 动画 ID | `typewriter` |
| soundEnabled | 是否播放所选动画的配套音轨 | `true` / `false` |
| soundGain | 配套轨总音量，范围 0–1 | `0.65`；`0` 为静音 |

可以直接复用 [with-sound.json](examples/with-sound.json)，或从画廊下载自己的配置。JSON 中的演示命令不会真实执行，软件操作也只是视频中的可编辑画面。

## 时间与替换内容

配套轨按 **场景 8 秒；一般动作从 0.6 秒开始，转场从 2.1 秒开始、基本时长 0.66 秒** 制作。整体移动这段镜头时，调整 HyperFrames 父镜头的 `data-start`，声画会一同移动。保持默认内部节奏即可更换支持范围内的内容。

逐字输入的每行使用固定 **1.2 秒** 输入预算，默认首行从 0.6 秒到 1.8 秒，确认键声在 1.8 秒。长命令缩短字间隔，短命令延长字间隔；单字先出现，再保持到确认时刻。默认声音针对一行命令，多行或其他内容结构仍需核对声音落点。

修改内部起点、持续时间、列表数量或动作节奏时，固定音轨不会自行重新编排。改变内部起点/时长且仍启用声音会得到配置错误；需要重新配轨，或先设置 `soundEnabled: false`。本版场景导出固定使用默认 8 秒节奏，支持内容、效果、常驻背景、声音开关与总音量。

## 13 个音效预设

| ID | 用途 |
|---|---|
| click-soft | 轻点、选择和焦点切换 |
| click | 按钮确认、点击反馈 |
| key-press | 单键、快捷键和输入确认 |
| typing | 终端和代码输入 |
| pop | 卡片、标签和节点出现 |
| ping | 数据落位、局部强调 |
| notification | 消息与通知到达 |
| chime | 成功、步骤完成 |
| whoosh-short | 短距离滑动 |
| whoosh | 面板切换、镜头聚焦 |
| sparkle | 高光、关键词强调 |
| error | 输入纠错、校验反馈 |
| riser | 数字递增、进度上升 |

这是 **13 个使用预设、11 个不同原始源声音**，不是 13 段独立录音。来源、许可声明和处理记录见 [CREDITS.md](assets/sfx/CREDITS.md)；波形与播放检查见 [音效验收](reports/sfx-quality.md)。

## 当前验收到哪里

已做浏览器解码/播放、PCM 波形量测、声画同步生命周期与 HyperFrames 媒体 probe 检查，详见 [audio-verification.json](reports/audio-verification.json) 和 [音效接入说明](reports/audio-integration-notes.md)。最新引擎整体状态见 [hyperframes-check-v2.json](reports/hyperframes-check-v2.json)，仍有未通过的对比度项目。

本次**未渲染视频，未做主观听音，也未验证与配音、BGM 的最终混音**。正式使用时加入真实人声与音乐，再按最终播放设备试听并调整 `soundGain`。
